<p>Let's write a new review</p>

<div class="container">
  <%= simple_form_for([@restaurant, @review]) do |f|%>
    <%= f.input :rating, collection: [1, 2, 3, 4, 5], label: "Rate us: " %>
    <%= f.input :content, as: :text, label: "What did you think?" %>
    <%= f.button :submit, class: "btn btn-primary  mb-3" %>
  <% end %>
</div>
